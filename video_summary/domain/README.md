# Domain

This directory contains the shared dataclasses that represent pipeline inputs, intermediate results, artifacts, and persisted state.

Use this package for types that need to move between interfaces, adapters, services, and pipeline steps without depending on a specific backend.
