# Services

This directory contains pure helper logic that does not need to talk to external tools directly.

The current services focus on transcript assembly and slide mapping so that adapters and pipeline steps can reuse the same business rules without duplicating them.
